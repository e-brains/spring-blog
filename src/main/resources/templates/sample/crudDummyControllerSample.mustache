{{>layout/header}}

<form action="/user/join" method="post">
    <!--    <div class="mb-3">-->
    <!--        <label class="form-label">ID</label>-->
    <!--        <input type="text" class="form-control" name="id"> &lt;!&ndash; DTO의 필드명과 동일한 이름을 부여 &ndash;&gt;-->
    <!--    </div>-->
    <div class="mb-3">
        <label class="form-label">이름</label>
        <input type="text" class="form-control" name="username"> <!-- DTO의 필드명과 동일한 이름을 부여 -->
    </div>
    <div class="mb-3">
        <label class="form-label">패스워드</label>
        <input type="text" class="form-control" name="password"> <!-- DTO의 필드명과 동일한 이름을 부여 -->
    </div>
    <div class="mb-3">
        <label class="form-label">이메일</label>
        <input type="text" class="form-control" name="email"> <!-- DTO의 필드명과 동일한 이름을 부여 -->
    </div>

    <button type="submit" class="btn btn-primary">Submit</button>
</form>

{{>layout/footer}}